const DATE_FORMAT = "DD/MM/YY";

const SESSION_MINUTES = 5;

const BASE_TOKEN = "token";

const EXP_DATA = {
  expToIncreaseLevel: 100,
  levelMilestone: 10,
  levelBaseReward: 30,
  totalLevel: 100,
};

const TOTAL_DAILY_BONUS_DAYS = 500;

const LOTTERY_REWARDS = {
  FIRST: 70,
  SECOND: 20,
  THIRD: 10,
};

export {
  BASE_TOKEN,
  DATE_FORMAT,
  EXP_DATA,
  LOTTERY_REWARDS,
  SESSION_MINUTES,
  TOTAL_DAILY_BONUS_DAYS,
};
